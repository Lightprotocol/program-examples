[package]
name = "native-program-burn"
version = "0.1.0"
description = "Native Solana program for burning compressed accounts"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "native_program_burn"

[features]
no-entrypoint = []
test-sbf = ["test-helpers"]
test-helpers = ["dep:light-client", "dep:solana-keypair", "dep:solana-instruction", "dep:solana-signer"]
default = []

[[test]]
name = "test"
required-features = ["test-helpers"]

[dependencies]
light-sdk = { version = "0.24.0", features = ["keccak"] }
light-hasher = "6.0.0"
light-macros = "3.0.0"
solana-program = "4.0"
borsh = "1.7"

# test-helpers deps are host-only (light-client/solana-keypair pull getrandom 0.2,
# which has no solana target). Keep them out of the on-chain (SBF) build so
# `cargo test-sbf` compiles the program.
[target.'cfg(not(target_os = "solana"))'.dependencies]
light-client = { version = "0.24.0", optional = true }
solana-keypair = { version = "3.1.2", optional = true }
solana-instruction = { version = "3.4", optional = true }
solana-signer = { version = "3.0", optional = true }

[dev-dependencies]
light-client = "0.24.0"
tokio = "1.49.0"
solana-instruction = "3.4"
solana-keypair = "3.1.2"
solana-pubkey = { version = "4.2", features = ["curve25519", "sha2"] }
solana-signature = "3.4"
solana-signer = "3.0"
five8_core = { version = "0.1.2", features = ["std"] }

[lints.rust.unexpected_cfgs]
level = "allow"
check-cfg = [
    'cfg(target_os, values("solana"))',
    'cfg(feature, values("frozen-abi", "no-entrypoint"))',
]
